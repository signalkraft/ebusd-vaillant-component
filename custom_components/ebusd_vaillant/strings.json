{
  "entity": {
    "water_heater": {
      "ebusd_water_heater": {
        "state": {
          "auto": "Auto",
          "day": "Day"
        }
      }
    }
  },
  "config": {
    "step": {
      "user": {
        "title": "ebusd Vaillant",
        "description": "Configure the ebusd MQTT integration. Make sure the MQTT integration is set up first.",
        "data": {
          "name": "Device name",
          "mqtt_prefix": "MQTT topic prefix"
        },
        "data_description": {
          "name": "Name shown in entity labels, e.g. \"Vaillant\" produces \"Vaillant Zone 1\"",
          "mqtt_prefix": "The root topic ebusd publishes to (default: ebusd)"
        }
      }
    },
    "error": {
      "mqtt_not_available": "The MQTT integration is not available. Please set it up first."
    },
    "abort": {
      "already_configured": "This ebusd prefix is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Configure ebusd Vaillant",
        "data": {
          "away_mode_duration": "Default away mode duration (days)",
          "quick_veto_duration": "Default quick veto duration (hours)",
          "max_zones": "Number of heating zones",
          "prime_poll_values": "Prime poll values on startup"
        },
        "data_description": {
          "away_mode_duration": "Number of days the away mode lasts when activated (default: 7)",
          "quick_veto_duration": "Number of hours the quick veto lasts when triggered (default: 3)",
          "max_zones": "Maximum number of heating zones to discover (1-4, default: 4)",
          "prime_poll_values": "Publish ?1 to /get topics on startup so ebusd polls all values at priority 1 immediately"
        }
      }
    }
  }
}
